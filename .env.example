# Documentation Base URL
# The base URL for online documentation to fetch from
# Default: https://www.synergex.com/docs/ (Synergy/DE docs)
# Examples:
#   - https://docs.python.org/3/
#   - https://reactjs.org/docs/
#   - https://developer.mozilla.org/en-US/docs/Web/
DOC_BASE_URL=https://www.synergex.com/docs/

# Default documentation version to use
# The version string depends on the documentation structure
# For Synergy/DE: "latest", "12.3", "11.1.1", etc.
# For other docs: check their version structure
# Default: "latest"
DOC_DEFAULT_VERSION=latest

# Optional: Path to local documentation directory
# If set, the server will use a Local→Online fallback strategy:
#   1. Try to read from local files first
#   2. Fallback to online documentation if local file not found
# The path must exist and be readable, or the server will fail to start.
# Leave empty to use online documentation only
#
# Examples:
#   - /path/to/synergex-docs
#   - /path/to/react-docs
#   - /path/to/python-docs
DOC_LOCAL_PATH=

# Cache directory for storing fetched and parsed topics
# Relative or absolute path to cache directory
# Default: ./cache
DOC_CACHE_DIR=./cache

# Logging level
# Options: debug, info, warn, error
# Default: info
LOG_LEVEL=info

# ============================================================================
# DEPRECATED: Old environment variable names (still supported for backward compatibility)
# ============================================================================
# These are deprecated and will be removed in a future version.
# Please use the new DOC_* names above instead.
#
# SYNERGYDE_DOC_BASE_URL → DOC_BASE_URL
# SYNERGYDE_DOC_DEFAULT_VERSION → DOC_DEFAULT_VERSION
# SYNERGYDE_LOCAL_DOC_PATH → DOC_LOCAL_PATH
# SYNERGYDE_CACHE_DIR → DOC_CACHE_DIR
